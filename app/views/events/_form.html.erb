<%= form_for(@event) do |f| %>
  <% if @event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% @event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :action %><br>
    <%= f.text_field :action %>
  </div>
  <div class="field">
    <%= f.label :eventable_id %><br>
    <%= f.number_field :eventable_id %>
  </div>
  <div class="field">
    <%= f.label :eventable_type %><br>
    <%= f.text_field :eventable_type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
